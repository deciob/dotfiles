-- This is about the little floating window that comes up when doing certain LSP operations.
-- The repo is now archived, but using it here for simplicity.

return {
	"stevearc/dressing.nvim",
	opts = {},
}
